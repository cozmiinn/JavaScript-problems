//get a age and convert in to a Number;
let age = Number(prompt("What is your age, buddy?"));

//if age is too small
if(age < 18) {
    console.log("You're so younggg !!");
}
//else, if age is too big
else {
    console.log("You're an adult, nice !");
}